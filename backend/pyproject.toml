[project]
name = "job-app-manager-backend"
version = "0.1.0"
description = "Job Application Manager backend"
requires-python = ">=3.12"

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.ruff]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
line-length = 100